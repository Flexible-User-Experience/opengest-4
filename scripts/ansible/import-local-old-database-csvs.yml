---
- hosts: s6
  gather_facts: false
  vars_files:
    - "{{ playbook_dir }}/vars/config.yml"
  tasks:
    - name: execute province CSV import by partner primary address to local database
      delegate_to: mother
      shell: "php bin/console app:import:province var/csv/partners.csv 9 10"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute province CSV import by partner secondary address to local database
      delegate_to: mother
      shell: "php bin/console app:import:province var/csv/partners.csv 19 20"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute province CSV import by enterprise to local database
      delegate_to: mother
      shell: "php bin/console app:import:province var/csv/enterprises.csv 5 6"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute province CSV import by operator to local database
      delegate_to: mother
      shell: "php bin/console app:import:province var/csv/operators.csv 12 12"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute city CSV import by partner primary address to local database
      delegate_to: mother
      shell: "php bin/console app:import:city var/csv/partners.csv 8 6 9 10"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute city CSV import by partner secondary address to local database
      delegate_to: mother
      shell: "php bin/console app:import:city var/csv/partners.csv 18 17 19 20"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute city CSV import by enterprise to local database
      delegate_to: mother
      shell: "php bin/console app:import:city var/csv/enterprises.csv 4 7 5 6"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute city CSV import by operator to local database
      delegate_to: mother
      shell: "php bin/console app:import:city var/csv/operators.csv 11 10 12 12"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise var/csv/enterprises.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise activity lines CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise:activity:line var/csv/enterprise_activity_lines.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise collection document types CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise:collection:document:type var/csv/enterprise_collection_document_types.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise group bountys CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise:group:bounty var/csv/enterprise_group_bountys.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise holidays CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise:holiday var/csv/enterprise_holidays.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute enterprise transfer accounts CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:enterprise:transfer:account var/csv/enterprise_transfer_accounts.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator var/csv/operators.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator checking type CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:checking:type var/csv/operator_checking_types.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator checking CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:checking var/csv/operator_checkings.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator absence type CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:absence:type var/csv/operator_absence_types.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator absence CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:absence var/csv/operator_absences.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator digital tachographs CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:digital:tachograph var/csv/operator_digital_tachographs.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute operator various amount CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:operator:various:amount var/csv/operator_various_amounts.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner class CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:class var/csv/partner_classes.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner type CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:type var/csv/partner_types.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner var/csv/partners.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner contact CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:contact var/csv/partner_contacts.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner unable days CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:unable:days var/csv/partner_unabled_days.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner building site CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:building:site var/csv/partner_building_sites.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute partner order CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:partner:order var/csv/partner_orders.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute setting sale invoice series CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:setting:sale:invoice:series var/csv/setting_sale_invoice_series.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute sale tariff CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:sale:tariff var/csv/sale_tariff.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
    - name: execute sale invoice CSV import to local database
      delegate_to: mother
      shell: "php bin/console app:import:sale:invoice var/csv/sale_invoice.csv"
      args:
        chdir: "{{ playbook_dir}}/../../"
