{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') %}
    <a href="{{ path('admin_app_sale_saledeliverynote_edit', {'id': object.id}) }}" class="btn btn-sm btn-default edit_link" title="{{ 'action_edit' | trans({}, 'SonataAdminBundle') }}"><i class="fa fa-pencil"></i></a>
{% endif %}
