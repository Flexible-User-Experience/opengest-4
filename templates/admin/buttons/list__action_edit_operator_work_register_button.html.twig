{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') %}
    <a href="{{ path('admin_app_operator_operatorworkregisterheader_create', {operator: object.operator.id, date: object.date|date("d-m-Y"), previousInputType: 'hour'}) }}"
       class="btn btn-sm btn-default edit_link" title="{{ 'action_edit' | trans({}, 'SonataAdminBundle') }}"
    >
        <i class="fa fa-pencil"></i>
    </a>
{% endif %}
