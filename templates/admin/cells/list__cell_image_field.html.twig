{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        {% if object.image %}
            <img src="{{ vich_uploader_asset(object, 'imageFile') | imagine_filter('60x60') }}" alt="thumbnail">
        {% else %}
            ---
        {% endif %}
    {% endspaceless %}
{% endblock %}
