{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        {% if object.logo %}
            <img src="{{ vich_uploader_asset(object, 'logoFile') | imagine_filter('60x60') }}" alt="thumbnail">
        {% else %}
            ---
        {% endif %}
    {% endspaceless %}
{% endblock %}
