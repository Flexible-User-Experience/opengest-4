{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% if object.vehicle.mainImage %}
        <img src="{{ vich_uploader_asset(object.vehicle, 'mainImageFile') | imagine_filter('60x60') }}" alt="thumbnail">
    {% else %}
        ---
    {% endif %}
{% endblock %}
