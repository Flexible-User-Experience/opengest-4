{% extends 'admin/edit.html.twig' %}

{# override javascript includes #}
{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript">
        jQuery(document).ready(function() {
          let purchaseInvoiceLinesNode = jQuery('#field_widget_{{ form.purchaseInvoiceLines.vars.id }}');
          purchaseInvoiceLinesNode.find("th").eq(1).attr('width','10%')
          purchaseInvoiceLinesNode.find("th").eq(5).css('width','110px')
          purchaseInvoiceLinesNode.find("th").eq(8).css('width','70px')
        })
    </script>
{% endblock %}

